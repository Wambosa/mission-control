node_modules
.codex
.claude
.cursor
.impeccable
dist
dist-electron
dist-server
.output
.pnpm-store
.vinxi
.vite
.tanstack
.DS_Store
*.sw?
.certs
*.log
.env
.env.local
src/routeTree.gen.ts
/release
.env.release
dist-electron-out/
artifacts/

.openai-key
.license-key

# Local dev SQLite store + app state (isolated from the installed app's DB)
.dev-userdata/

# Mission Control Recall (project memory) — private, do not commit
CLAUDE.local.md
# Mission Control Recall (code graph MCP) — machine-specific, regenerated per session
.mcp.json

# Grok goal scratch dirs (local agent workspaces)
Temp/
**/grok-goal-*/

# Local git worktrees created by Mission Control
.worktree/
